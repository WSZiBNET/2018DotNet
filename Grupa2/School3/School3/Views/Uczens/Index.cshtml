@model IEnumerable<School3.Models.Uczen>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Nazwisko)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Rocznik)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Klasa)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nazwisko)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rocznik)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Klasa)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Imie">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Imie">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Imie">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
